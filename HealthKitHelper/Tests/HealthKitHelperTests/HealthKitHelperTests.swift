    import XCTest
    @testable import HealthKitHelper

    final class HealthKitHelperTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(HealthKitHelper().text, "Hello, World!")
        }
    }
